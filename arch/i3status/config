general {
    output_format = i3bar
    colors = true
    interval = 1
}

order += "volume master"
order += "time"

volume master {
    format = "%volume"
    format_muted = "M"
    device = "default"
    mixer = "Master"
    mixer_idx = 0
}

time {
  format = "%Y-%m-%d %H:%M:%S"
}
